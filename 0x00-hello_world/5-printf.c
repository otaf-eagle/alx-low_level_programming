#include <stdio.h>
/**
 * main function will take void argument , using the standard input output 
 * the printf function will be then printing out the string , with proper grammar, but the outcome is a piece of art,
 * finally returning a 0
 */
int main(void)
{
	printf("%s","with proper grammar, but the outcome is a piece of art,\n");
		return 0;
}
